// Given a string, convert all Latin capital letters of the string to lowercase.

const string: string = "A, B, C, D, E, F";
console.log(string);

const smallLetters: string = string.toLowerCase();
console.log(smallLetters);
